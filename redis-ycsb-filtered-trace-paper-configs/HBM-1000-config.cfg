########################
# Example config file
# Comments start with #
# There are restrictions for valid channel/rank numbers
 standard = HBM
 channels = 8
 ranks = 1
 speed = HBM_1Gbps
 org = HBM_4Gb
 translation = Random
 record_cmd_trace = off
#
########################

### Below are parameters only for CPU trace
 cpu_frequency = 4000
 cache = L3
 expected_limit_insts = 900000000
########################
